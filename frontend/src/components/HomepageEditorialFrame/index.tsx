import styled from 'styled-components';
import { homepageTokens } from '../../styles/theme';

export const HomepageEditorialFrame = styled.div`
  width: min(100%, ${homepageTokens.editorialContentFrameWidth});
  margin-inline: auto;
`;
